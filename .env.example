# Site Configuration
VITE_SITE_URL=https://lookup.hivetalk.org
VITE_SITE_DISPLAY_NAME=The Lookup

# You can customize these values for your deployment

# Note: Some static files like public/site.webmanifest and public/_redirects
# contain hardcoded references and may need manual updates for custom deployments.

# Navigation Configuration
# Control which sections appear in the main navigation menu
# Available sections: apps, resources, nips, repositories, dvm
# Leave empty to show all sections, or specify comma-separated list
# Examples:
#   VITE_SECTIONS=apps,resources          (only show Apps and Resources)
#   VITE_SECTIONS=apps,dvm                (only show Apps and DVM)
#   VITE_SECTIONS=apps                    (only show Apps)
#   VITE_SECTIONS=                        (show all sections - default)
VITE_SECTIONS=

# Relay Configuration
# Default relay URL for app submissions and general operations
# Example: VITE_RELAY_URL=wss://relay.nostr.net
VITE_RELAY_URL=wss://relay.nostr.net

# Relay npub for app ownership claims
# Add the relay's npub here to enable ownership claiming from relay accounts
# Example: VITE_RELAY_NPUB=npub1...
VITE_RELAY_NPUB=npub18pudjhdhhp2v8gxnkttt00um729nv93tuepjda2jrwn3eua5tf5s80a699

# Lightning Payment Configuration for App Submission
# Enable or disable payment requirement for new app submissions
# Set to 'true' to require payment, 'false' or empty to disable
# Example: VITE_SUBMIT_APP_PAYMENT_ENABLED=true
VITE_SUBMIT_APP_PAYMENT_ENABLED=

# Lightning address that supports NIP-57 (zap receipts) for receiving payments
# Only used if VITE_SUBMIT_APP_PAYMENT_ENABLED=true
# Example: VITE_SUBMIT_APP_LIGHTNING_ADDRESS=payments@example.com
VITE_SUBMIT_APP_LIGHTNING_ADDRESS=

# Fee amount in satoshis for submitting new apps (only applies to new submissions, not edits)
# Only used if VITE_SUBMIT_APP_PAYMENT_ENABLED=true
# Example: VITE_SUBMIT_APP_FEE=1000
VITE_SUBMIT_APP_FEE=

# Additional relay for zap receipt verification (optional)
# Some Lightning services publish zap receipts to specific relays
# Example: VITE_ZAP_RECEIPT_RELAY=wss://relay.primal.net
VITE_ZAP_RECEIPT_RELAY=

# Business listing payments
VITE_SUBMIT_LISTING_PAYMENT_ENABLED=true
VITE_SUBMIT_LISTING_LIGHTNING_ADDRESS=your-listing-payments@example.com
VITE_SUBMIT_LISTING_FEE=500   # for example

# Client-side Content Curation
VITE_SITE_NAME=lookup-client # use this for the client tag to show only entries created by your client. if no tag, then everything on VITE_RELAY_URL is shown
# Enable client-side filtering of flagged content based on trusted curator flags
VITE_CLIENT_SIDE_CURATION=false
# Comma-separated list of npub keys for trusted curators whose kind 1984 flags will hide content
# Example: npub1abc...,npub1def...,npub1ghi...
VITE_NPUB_CURATORS=npub18pudjhdhhp2v8gxnkttt00um729nv93tuepjda2jrwn3eua5tf5s80a699

# Debug Logging
# Enable debug console logs for development/troubleshooting
VITE_DEBUG_LOGGING=false
